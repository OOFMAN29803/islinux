# islinux
Simple way to use WSL instead of opening like a new command you can run seamlessly (will check if windows or not so you can just use it universially and if it isn't windows it kinda auto converts)

## Prerequisites
- WSL (Needed) it will tweak out and get angry because the backbone is WSL.
- Python3 (WSL) You need it as it fully is in Linux (This includes imports for now (although I will change it)
- Windows Why would you be using this with linux installed as your main OS
- Computer Unless you are emulating windows on like a I forgot what it was that one phone running native W11, you need one, preferrably with a CPU

# What you need to do to run WSL (ooooOOooOh very complicated)
```python
from islinux import islinux
islinux()
```
(Its incredibly complicated, you wouldn't understand...)



## Standard MIT License Applies :)
